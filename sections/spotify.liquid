<style>
  .spotify-embed-{{ section.id }}-wrapper {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
    margin-top: {{ section.settings.margin_top }}px;
    margin-bottom: {{ section.settings.margin_bottom }}px;
  }

  .spotify-embed-{{ section.id }}-heading {
    margin-bottom: 0.5rem;

    {% if section.settings.heading_size == 's' %}
      font-size: 18px;
    {% elsif section.settings.heading_size == 'm' %}
      font-size: 24px;
    {% elsif section.settings.heading_size == 'l' %}
      font-size: 32px;
    {% elsif section.settings.heading_size == 'xl' %}
      font-size: 44px;
    {% endif %}
  }

  .spotify-embed-{{ section.id }}-text {
    margin-bottom: 1rem;
    line-height: 1.6;
  }

  /* Product-slider style button */
  .spotify-share-btn {
    margin-top: 20px;
    padding: 12px 22px;
    font-size: 14px;
    background: transparent;
    border: 1px solid #1c1c1c;
    color: #1c1c1c;
    border-radius: 0px;
    cursor: pointer;
    letter-spacing: 0.3px;
    transition: all .18s ease;
  }

  .spotify-share-btn:hover {
    background: #1c1c1c;
    color: white;
  }
</style>

<div class="spotify-embed-{{ section.id }}-wrapper">

  {% if section.settings.heading != blank %}
    <h1 class="spotify-embed-{{ section.id }}-heading">
      {{ section.settings.heading }}
    </h1>
  {% endif %}

  {% if section.settings.description != blank %}
    <p class="spotify-embed-{{ section.id }}-text">
      {{ section.settings.description }}
    </p>
  {% endif %}

  <div id="spotify-embed-container-{{ section.id }}">
    {{ section.settings.embed_code | raw }}
  </div>

  <button 
    class="spotify-share-btn"
    id="spotify-share-btn-{{ section.id }}">
    Share playlist
  </button>

</div>

<script>
document.addEventListener("DOMContentLoaded", function () {
  const iframe = document.querySelector("#spotify-embed-container-{{ section.id }} iframe");

  if (!iframe) return;

  const shareBtn = document.querySelector("#spotify-share-btn-{{ section.id }}");

  shareBtn.addEventListener("click", async () => {

    const embedUrl = iframe.getAttribute("src");

    // Turn /embed/ link into the correct share link
    const shareUrl = embedUrl.replace("/embed/", "/");

    if (navigator.share) {
      try {
        await navigator.share({
          title: "Spotify Playlist",
          url: shareUrl
        });
      } catch (err) {
        console.log("Share canceled");
      }
    } else {
      navigator.clipboard.writeText(shareUrl);
      shareBtn.innerText = "Copied!";
      setTimeout(() => shareBtn.innerText = "Share playlist", 1500);
    }
  });
});
</script>

{% schema %}
{
  "name": "Spotify Embed",
  "settings": [
    {
      "type": "header",
      "content": "Text"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Featured Playlist"
    },
    {
      "type": "textarea",
      "id": "description",
      "label": "Description",
      "default": "Enjoy this curated Spotify playlist."
    },
    {
      "type": "select",
      "id": "heading_size",
      "label": "Heading size",
      "options": [
        { "label": "Small", "value": "s" },
        { "label": "Medium", "value": "m" },
        { "label": "Large", "value": "l" },
        { "label": "XL", "value": "xl" }
      ],
      "default": "m"
    },

    {
      "type": "header",
      "content": "Spotify Embed"
    },
    {
      "type": "textarea",
      "id": "embed_code",
      "label": "Paste Spotify iframe",
      "default": "<iframe style='border-radius:12px' src='https://open.spotify.com/embed/playlist/xxxx' width='100%' height='352' frameborder='0' allow='autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture' loading='lazy'></iframe>"
    },

    {
      "type": "header",
      "content": "Spacing"
    },
    {
      "type": "range",
      "id": "margin_top",
      "label": "Top spacing",
      "min": 0,
      "max": 100,
      "default": 20
    },
    {
      "type": "range",
      "id": "margin_bottom",
      "label": "Bottom spacing",
      "min": 0,
      "max": 100,
      "default": 20
    }
  ],
  "presets": [
    {
      "name": "Spotify Embed"
    }
  ]
}
{% endschema %}
